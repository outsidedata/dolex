/**
 * `dolex analyze` — auto-generate an analysis plan: 4-6 prioritized steps, each
 * with a ready-to-run SQL query and a suggested chart pattern.
 */
export declare function analyzeCommand(argv: string[]): Promise<number>;
